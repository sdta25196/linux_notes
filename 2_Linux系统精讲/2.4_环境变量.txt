1、如果自己有需要定义的别名，则可以写在自己的家目录下的.bashrc文件里面 ~/.bashrc

2、which查看命令的绝对路径和PATH变量有关
	一般情况下，环境变量中的文件都是带有执行权限的，比如你写了一个shell脚本，放在了/usr/local/bin下，但是却没有执行的权限，那么你直接输入这个文件名在命令行中，是不可以执行的，当然用which命令也无法找到

3、/etc/profile都是系统开机会初始化的一些变量，可以把path变量写在这个地方，重启的时候就会自动加载了。也可以不用重启，直接使用source /etc/profile.
